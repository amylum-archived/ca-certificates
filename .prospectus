item do
  expected do
    url_xpath
    url 'https://www.archlinux.org/packages/core/any/ca-certificates/'
    xpath '//*[@id="pkgdetails"]/h2'
    regex /^ca-certificates ([\w.]*)-\d+$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
